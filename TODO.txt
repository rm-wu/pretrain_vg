TODO LIST:
1) Semplificare e utilizzare l'average pooling delle reti di torchvision
    - modificare il numero di classi in base al dataset di pretraining?
        La rete di torchvision viene troncata all'average pooling, per cui usando
            1) gldv2  --> le features vengono mandate a ArcMarginProduct --> CE Loss                        FATTO
            2) places --> le features passano ad un Linear che mappa da dim_features a num_classes(=365)    FATTO

2) Integrare a benchmarking_vg il caricamento della rete pretrainata (vedi benchmarking_vg/z_layers/network.py@61)

3) GLDv2 --> la rete viene trainata per N epoche (con N uguale 5/6)

4) Places365 --> Il modello viene trainato a convergenza
(la differenza rispetto a prima sta nel pooling che nella ora è nn.AdaptiveAvgPool2 di torchvision)